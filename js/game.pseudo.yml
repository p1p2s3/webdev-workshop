- When a question loads:
    - update the question text
    - update the answer buttons text
    - fetch the clue image

- When user clicks any answer button:
    - check if the answer is correct and trigger "answer is correct" or "answer is wrong" event appropriately

- When answer is correct:
    - change the button colour to green for 1s
    - increment level number on top
    - play the animation for 1s
    - load next question

    
- When answer is wrong:
    - change the button colour to red
    - If number of hearts>0:
            decrement hearts
            next question
      else:
            display "game over!"
            reload next game

- when webpage loads:
    -fetch question

- when image loads:
    - update the clue image

